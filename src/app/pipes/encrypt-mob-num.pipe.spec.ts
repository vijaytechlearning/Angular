import { EncryptMobNumPipe } from './encrypt-mob-num.pipe';

describe('EncryptMobNumPipe', () => {
  it('create an instance', () => {
    const pipe = new EncryptMobNumPipe();
    expect(pipe).toBeTruthy();
  });
});
